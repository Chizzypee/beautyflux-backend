const ProfileModel = require("../models/account");

exports.updateProfile = async(req, res) =>{
    try {
        const {fileData} = req.body;
        if(fileData){
            
        }
    } catch (error) {
        
    }
}